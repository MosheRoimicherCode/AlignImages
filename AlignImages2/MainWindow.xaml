<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AlignImages2.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Align Images">

    <Grid Padding="24" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Border
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            MaxWidth="620"
            Padding="20"
            CornerRadius="10"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

            <StackPanel Spacing="12">
                <TextBlock Text="Align Images" FontSize="24" FontWeight="SemiBold" />

                <Button x:Name="SelectFolderButton"
                        Content="Select Folder"
                        Click="SelectFolderButton_Click"
                        MinHeight="40" />

                <TextBlock x:Name="SelectedFolderTextBlock"
                           Text="No folder selected."
                           TextWrapping="WrapWholeWords" />

                <Button x:Name="AlignButton"
                        Content="Align Images"
                        Click="AlignImagesButton_Click"
                        MinHeight="40" />

                <TextBlock Text="Progress" FontWeight="SemiBold" />
                <ProgressBar x:Name="MyProgressBar" Minimum="0" Maximum="100" Value="0" Height="12" />
                <TextBlock x:Name="ProgressPercentTextBlock" Text="0 %" />

                <Grid ColumnSpacing="12" RowSpacing="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="170" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Start time:" />
                    <TextBlock x:Name="StartTimeValueTextBlock" Grid.Row="0" Grid.Column="1" Text="-" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="End time:" />
                    <TextBlock x:Name="EndTimeValueTextBlock" Grid.Row="1" Grid.Column="1" Text="-" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Time taken:" />
                    <TextBlock x:Name="ElapsedValueTextBlock" Grid.Row="2" Grid.Column="1" Text="00:00:00" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Should take (est.):" />
                    <TextBlock x:Name="EstimatedTotalValueTextBlock" Grid.Row="3" Grid.Column="1" Text="-" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Time left (est.):" />
                    <TextBlock x:Name="EstimatedRemainingValueTextBlock" Grid.Row="4" Grid.Column="1" Text="-" />
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>
