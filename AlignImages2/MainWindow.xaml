<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AlignImages2.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlignImages2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="AlignImages2"
    >

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"  />
            <RowDefinition Height="100" />
            <RowDefinition Height="100" />
            <RowDefinition Height="*"  />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>


        <DropDownButton x:Name="FilePickerButton" Content="Import Photos" Grid.Row="1" Grid.Column="1">
            <DropDownButton.Flyout>
                <MenuFlyout Placement="Bottom">
                    <MenuFlyoutItem Text="Select Files" Click="PickMultipleFilesFlyButton_Click"/>
                    <MenuFlyoutItem Text="Select Folder" Click="PickFolderFlyButton_Click"/>
                </MenuFlyout>
            </DropDownButton.Flyout>
        </DropDownButton>
        <TextBlock x:Name="PickedMultipleFilesTextBlock" Text="No files picked" Grid.Row="2" Grid.Column="1"/>
        <Button Content="Align Images" Grid.Row="1" Grid.Column="2" Click="AlignImagesButton_Click"/>

        <ProgressBar x:Name="MyProgressBar" 
             Grid.Row="3" 
             Grid.Column="0" 
             Grid.ColumnSpan="4" 
             Minimum="0" 
             Maximum="100" 
             Value="0" 
             HorizontalAlignment="Stretch" 
             VerticalAlignment="Center"
             Margin="20,0,20,0"/>

    </Grid>
</Window>
