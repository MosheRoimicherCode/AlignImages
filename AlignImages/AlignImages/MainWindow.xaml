<?xml version="1.0" encoding="utf-8"?>
<ex:WindowEx
    xmlns:ex="using:WinUIEx"
    x:Class="AlignImages.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlignImages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    Width="800" Height="600"
    mc:Ignorable="d"
    Title="AlignImages">

    <ex:WindowEx.SystemBackdrop>
        <MicaBackdrop />
    </ex:WindowEx.SystemBackdrop>

    <Grid>
        <Rectangle Fill="#FF111119" Opacity="0.85"/>

        <NavigationView x:Name="MainNavView" 
                        PaneDisplayMode="Auto" 
                        IsSettingsVisible="False"
                        PaneTitle="GimbalLogic Pro"
                        Background="Transparent">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Home" IsSelected="True"/>
                <NavigationViewItem Icon="Folder" Content="Import Folder" Tapped="ImportFolder_Tapped"/>
            </NavigationView.MenuItems>

            <Grid Padding="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Margin="0,0,20,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Analyzed Images" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20"/>

                    <controls:DataGrid x:Name="Image DataGrid" 
                                       Grid.Row="1"
                                       AutoGenerateColumns="False"
                                       IsReadOnly="True"
                                       GridLinesVisibility="Horizontal"
                                       Background="#55000000"
                                       RowBackground="Transparent">
                        <controls:DataGrid.Columns>
                            <controls:DataGridTemplateColumn Header="Thumbnail" Width="110">
                                <controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn.CellTemplate>
                            </controls:DataGridTemplateColumn>
                            <controls:DataGridTextColumn Header="Filename" Binding="{Binding FileName}" Width="*"/>
                            <controls:DataGridTextColumn Header="Roll (°)" Binding="{Binding DetectedRoll}" Width="100"/>
                            <controls:DataGridTextColumn Header="Pitch Mode" Binding="{Binding PitchMode}" Width="130"/>
                            <controls:DataGridTextColumn Header="Action Needed" Binding="{Binding ActionNeeded}" Width="160"/>
                            <controls:DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                        </controls:DataGrid.Columns>
                    </controls:DataGrid>
                </Grid>

                <StackPanel Grid.Column="1" Background="#66000000" Padding="20" CornerRadius="8" BorderBrush="#33FFFFFF" BorderThickness="1">
                    <TextBlock Text="Preview &amp; Action" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,20"/>

                    <Grid Height="180" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <Image Source="Assets/StoreLogo.png" Stretch="UniformToFill" Height="140" RenderTransformOrigin="0.5,0.5">
                                <Image.RenderTransform>
                                    <RotateTransform Angle="90"/>
                                </Image.RenderTransform>
                            </Image>
                            <TextBlock Text="Before" HorizontalAlignment="Center" Margin="0,5,0,0" Opacity="0.7"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="→" VerticalAlignment="Center" FontSize="24" Margin="10,0"/>

                        <StackPanel Grid.Column="2">
                            <Image Source="Assets/StoreLogo.png" Stretch="UniformToFill" Height="140"/>
                            <TextBlock Text="After" HorizontalAlignment="Center" Margin="0,5,0,0" Opacity="0.7"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock x:Name="SummaryText" Text="Ready to process items." Margin="0,0,0,10" Opacity="0.8"/>

                    <Button x:Name="ProcessBtn" Content="Process Batch (via jpegtran)" 
                            Style="{StaticResource AccentButtonStyle}" 
                            HorizontalAlignment="Stretch" 
                            Height="50" 
                            Click="ProcessBtn_Click"/>

                    <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" Margin="0,20,0,0">
                        <ProgressBar x:Name="MainProgressBar" Maximum="100" Value="0" Height="10" Margin="0,0,0,5"/>
                        <TextBlock x:Name="ProgressText" Text="Processing... 0%" HorizontalAlignment="Right" FontSize="12" Opacity="0.7"/>
                    </StackPanel>

                </StackPanel>
            </Grid>
        </NavigationView>
    </Grid>
</ex:WindowEx>

    